{% from 'BtnCrudBundle::breadcrumb.html.twig' import breadcrumb %}
{% extends '::control.html.twig' %}

{% form_theme form 'BtnCrudBundle:Form:fields.html.twig' %}

{% block content %}

    <form action="{{ path('cp_newscategory_create') }}" method="post" {{ form_enctype(form) }} class="well form-horizontal" novalidate>
          <fieldset>
              <legend>{% trans %}news_category.create{% endtrans %}</legend>
              {{ form_widget(form) }}
              <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                  <button type="submit" class="btn btn-success">{% trans %}crud.create{% endtrans %}</button>
              </div>
            </div>
        </fieldset>
    </form>

            <a href="{{ path('cp_newscategory') }}" class="btn">
        &laquo; {% trans %}crud.back{% endtrans %}
    </a>

{% endblock %}

{% block breadcrumb %}
    {{ breadcrumb({'cp_newscategory': 'crud.breadcrumb.list', 'cp_newscategory_new': 'crud.breadcrumb.new'}) }}
{% endblock %}
